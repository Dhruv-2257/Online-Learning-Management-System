<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>LearnEase LMS</title>
  <!-- CSS -->
  <link rel="stylesheet" href="/css/styles.css">
  <!-- Dark mode toggle handler -->
  <script>
    // Check for saved theme preference or use system preference
    const getTheme = () => {
      const storedTheme = localStorage.getItem('theme');
      if (storedTheme) return storedTheme;
      
      return window.matchMedia('(prefers-color-scheme: dark)').matches 
        ? 'dark' 
        : 'light';
    };
    
    // Apply theme class to html element
    const setTheme = (theme) => {
      document.documentElement.setAttribute('data-theme', theme);
      localStorage.setItem('theme', theme);
    };
    
    // Set theme on initial load
    setTheme(getTheme());
  </script>
</head>
<body>
  <header class="header">
    <div class="container">
      <div class="header-content">
        <div class="logo">
          <a href="/">LearnEase</a>
        </div>
        <nav class="main-nav">
          <ul>
            <li><a href="/" class="<%= path === '/' ? 'active' : '' %>">Courses</a></li>
            <% if (user) { %>
              <li><a href="/my-learning" class="<%= path === '/my-learning' ? 'active' : '' %>">My Learning</a></li>
              <% if (user.role === 'admin') { %>
                <li><a href="/admin" class="<%= path === '/admin' ? 'active' : '' %>">Admin</a></li>
              <% } %>
            <% } %>
          </ul>
        </nav>
        <div class="nav-controls">
          <div class="theme-toggle">
            <button id="theme-toggle-btn" aria-label="Toggle theme">
              <span class="light-icon">☀️</span>
              <span class="dark-icon">🌙</span>
            </button>
          </div>
          <div class="auth-buttons">
            <% if (user) { %>
              <span class="user-name"><%= user.username %></span>
              <button class="btn btn-outline" id="logout-btn">Logout</button>
            <% } else { %>
              <a href="/auth" class="btn btn-primary">Login</a>
            <% } %>
          </div>
          <button class="mobile-menu-btn" id="mobile-menu-btn">
            <span></span>
            <span></span>
            <span></span>
          </button>
        </div>
      </div>
    </div>
  </header>
  
  <div class="mobile-menu" id="mobile-menu">
    <div class="container">
      <ul>
        <li><a href="/" class="<%= path === '/' ? 'active' : '' %>">Courses</a></li>
        <% if (user) { %>
          <li><a href="/my-learning" class="<%= path === '/my-learning' ? 'active' : '' %>">My Learning</a></li>
          <% if (user.role === 'admin') { %>
            <li><a href="/admin" class="<%= path === '/admin' ? 'active' : '' %>">Admin</a></li>
          <% } %>
        <% } %>
      </ul>
    </div>
  </div>
  
  <main>